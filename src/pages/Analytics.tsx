import {
  Box,
  Typography,
} from '@mui/material';
const Analytics = () => {
  return (
    <Box
      sx={{
        height: '100%',
        bgcolor: '#f9fafb',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        p: 2,
      }}
    >
    <Typography variant="h6">Analytics</Typography>
    </Box>
  );
};

export default Analytics;
